SELECT
    MERCHANT_ID,
    MERCHANT_NM,
    MARKET_NM,
    BIZ_NO,
    APPLY_START_DT,
    APPLY_END_DT,
    USE_YN,
    REG_DTM,
    UPD_DTM
FROM TB_TRAD_MARKET_MERCHANT
WHERE :base_date BETWEEN APPLY_START_DT AND IFNULL(APPLY_END_DT, '99991231')
  AND USE_YN = 'Y'
