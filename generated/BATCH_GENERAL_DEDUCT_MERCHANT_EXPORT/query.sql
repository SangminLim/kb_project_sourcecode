SELECT
    MERCHANT_ID,
    MERCHANT_NM,
    BIZ_NO,
    MCC_CD,
    APPLY_START_DT,
    APPLY_END_DT,
    USE_YN,
    REG_DTM,
    UPD_DTM
FROM TB_GENERAL_DEDUCT_MERCHANT
WHERE :base_date BETWEEN APPLY_START_DT AND IFNULL(APPLY_END_DT, '99991231')
  AND USE_YN = 'Y'
